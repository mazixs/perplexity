name: Build, Check and Publish PKGBUILD

on:
  push:
    tags:
      - '*'
  pull_request:
    paths:
      - 'aur/**'
      - '.github/workflows/aur.yml'
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4.2.2

      - name: Update PKGBUILD version for tags
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          # Получаем версию из тега
          TAG_VERSION="${GITHUB_REF_NAME#v}"
          echo "🏷️ Updating to version: $TAG_VERSION"
          
          # Обновляем только версию - source array уже корректный
          sed -i "s/pkgver=.*/pkgver=$TAG_VERSION/" aur/PKGBUILD
          sed -i 's/pkgrel=.*/pkgrel=1/' aur/PKGBUILD
          
          echo "✅ Updated PKGBUILD version to $TAG_VERSION"
          echo "📋 Current source array:"
          grep -A 10 "source=(" aur/PKGBUILD

      - name: Publish to AUR
        if: startsWith(github.ref, 'refs/tags/')
        uses: KSXGitHub/github-actions-deploy-aur@v4.1.1
        with:
          pkgname: perplexity
          # Action сам найдет файл в подпапке
          pkgbuild: aur/PKGBUILD
          # Убираем .SRCINFO - action сгенерирует его автоматически
          assets: |
            aur/launcher.sh
            aur/perplexity.desktop
            aur/perplexity.png
          # Action сам обновит checksums и протестирует
          updpkgsums: true
          test: true
          test_flags: '--clean --cleanbuild --syncdeps --noconfirm'
          commit_username: ${{ secrets.AUR_USERNAME }}
          commit_email: ${{ secrets.AUR_EMAIL }}
          ssh_private_key: ${{ secrets.AUR_SSH_PRIVATE_KEY }}
          commit_message: "Update to ${{ github.ref_name }}"