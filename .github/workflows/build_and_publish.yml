name: build-and-publish-legacy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow notice
        run: |
          echo "build_and_publish.yml is kept only as a manual placeholder during the CI/CD migration."
          echo "Use ci.yml for validation on main/PRs and release.yml for tag-driven releases."
