name: Build PKGBUILD

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  actions: write
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: archlinux:latest
    steps:
      - name: Install packages
        run: pacman -Syy -q --noconfirm && pacman -S -q --noconfirm git sudo base-devel nodejs npm
        
      - name: Checkout repository
        uses: actions/checkout@v4.2.2
        with:
          persist-credentials: false

      - name: Verify PKGBUILD exists
        run: |
          echo "🔍 Checking PKGBUILD..."
          if [[ ! -f "aur/PKGBUILD" ]]; then
            echo "❌ PKGBUILD not found"
            exit 1
          fi
          echo "✅ PKGBUILD found"
          echo "📁 Current directory contents:"
          ls -la
          echo "📁 AUR directory contents:"
          ls -la aur/

      - name: Test PKGBUILD syntax
        run: |
          echo "🧪 Testing PKGBUILD syntax..."
          bash -n aur/PKGBUILD
          echo "✅ PKGBUILD syntax is valid"

      - name: Fix permissions for build
        run: |
          mkdir /home/build
          chgrp nobody /home/build
          chmod g+ws /home/build
          chown nobody .
          mkdir -p arch
          chown nobody arch
          # Fix npm cache permissions
          mkdir -p /.npm
          chown -R nobody:nobody /.npm
          chmod -R 755 /.npm

      - name: Build and test package
        run: |
          echo "📦 Building package..."
          # Copy all necessary files to build directory (fixes act-cli mounting issue)
          cp aur/* arch/ 2>/dev/null || true
          cd arch
          echo "📁 Contents of build directory:"
          ls -la
          echo "🧪 Testing PKGBUILD with bash..."
          bash -n PKGBUILD
          echo "✅ PKGBUILD syntax test passed"
          
          # Build package with makepkg
          echo "🔨 Building with makepkg..."
          sudo -u nobody makepkg --nobuild --nodeps --skipinteg
          echo "✅ Build and test step completed successfully"

      - name: Build full package for release
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          echo "📦 Building full package for release..."
          cd arch
          sudo -u nobody makepkg -fs --noconfirm
          ls -lh *.pkg.tar.zst
          # Get the exact package name
          PKG_FILE=$(ls *.pkg.tar.zst | head -n1)
          if [[ -z "$PKG_FILE" ]]; then
            echo "❌ No package file found"
            exit 1
          fi
          echo "PACKAGE_FILE=$PKG_FILE" >> $GITHUB_ENV
          echo "📦 Package file: $PKG_FILE"
          mv "$PKG_FILE" ../

      - name: Build full package for manual dispatch
        if: github.event_name == 'workflow_dispatch'
        run: |
          echo "📦 Building full package for testing..."
          cd arch
          sudo -u nobody makepkg -fs --noconfirm
          ls -lh *.pkg.tar.zst
          mv *.pkg.tar.zst ../

      - name: Upload package artifact (manual dispatch only)
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v4.6.2
        with:
          name: perplexity-pkg-${{ github.sha }}
          path: "*.pkg.tar.zst"
          retention-days: 7

      - name: Verify package file exists
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          if [ ! -f "${{ env.PACKAGE_FILE }}" ]; then
            echo "Error: Package file ${{ env.PACKAGE_FILE }} not found!"
            ls -la
            exit 1
          fi
          echo "Package file found: ${{ env.PACKAGE_FILE }}"
          ls -la "${{ env.PACKAGE_FILE }}"

      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2.3.2
        with:
          files: ${{ env.PACKAGE_FILE }}
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}