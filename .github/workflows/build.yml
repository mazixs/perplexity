name: Build & Test PKGBUILD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  actions: write

jobs:
  build:
    name: Build & Test
    runs-on: ubuntu-latest
    container:
      image: archlinux:latest

    steps:
      # 1. Системные зависимости (root)
      - name: Install base system deps
        run: |
          pacman -Syu --noconfirm git base-devel nodejs npm sudo

      # 2. Клонируем репозиторий
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      # 3. Создаём непривилегированного пользователя для makepkg
      - name: Add builder user
        run: |
          useradd -m builder
          echo "builder ALL=(ALL) NOPASSWD: ALL" >> /etc/sudoers
          chown -R builder:builder .

      # 4. Быстрая проверка наличия и синтаксиса PKGBUILD
      - name: Verify PKGBUILD & .SRCINFO
        run: |
          for f in aur/PKGBUILD aur/.SRCINFO; do
            [[ -f "$f" ]] || { echo "❌ Missing $f"; exit 1; }
          done
          bash -n aur/PKGBUILD
          echo "✅ PKGBUILD syntax OK"

      # 5. Подготовка build/ и создание source-tarball
      - name: Prepare build directory + source tarball
        run: |
          rm -rf build && mkdir build
          cp -r aur/* build/
          # --- создаём perplexity-<version>.tar.gz в build/ ---
          VERSION=$(grep '^pkgver=' build/PKGBUILD | cut -d= -f2)
          tar czf build/perplexity-${VERSION}.tar.gz \
              --transform "s,^,perplexity-${VERSION}/," \
              -C src .
          # -------------------------------------------
          chown -R builder:builder build
          echo "📂 build/ contents:"
          ls -1 build

      # 6. dry-run: проверяем доступность source-файлов
      - name: makepkg --nobuild (no deps)
        run: |
          runuser -u builder -- bash -lc \
            "cd build && makepkg --nobuild --nodeps --skipinteg"
          echo "✅ Source files OK"

      # 7. Создание бинарного пакета
      - name: Build Arch package
        run: |
          runuser -u builder -- bash -lc \
            "cd build && makepkg -f --nodeps --skipinteg --nocheck"
          echo "✅ Package built successfully"
          ls -lh build/*.pkg.tar.zst

      # 8. Артефакт (manual only)
      - name: Upload build artifact
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v4
        with:
          name: build-pkg-${{ github.run_id }}
          path: build/*.pkg.tar.zst
          retention-days: 7

      # 9. Загрузка бинарного пакета в релиз GitHub (только для main)
      - name: Upload binary package to release
        if: github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v2.3.2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          files: build/*.pkg.tar.zst
          tag_name: binary-${{ github.sha }}
          name: Binary Release ${{ github.sha }}
          generate_release_notes: true
          draft: false
          prerelease: false