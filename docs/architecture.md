# Архитектура проекта Perplexity Native (Linux)

## Цель
Автоматизировать превращение Windows-версии Perplexity в нативное Electron-приложение для Linux с полностью автоматизированной публикацией в AUR (Arch Linux User Repository) через CI/CD пайплайн.

## Текущая версия
- **Версия**: 1.1.3
- **Релиз**: 2 (deploy_aur)
- **Статус**: Стабильная с полной автоматизацией CI/CD

---

## Архитектура автоматизации

### 1. Подготовка исходников
- **Извлечение**: Windows-версия Perplexity распаковывается через 7z/innoextract
- **Обработка**: `npx asar extract app.asar app-src` для получения исходников
- **Размещение**: Содержимое копируется в `src/` для интеграции с Electron

### 2. Двойная система сборки
#### Исходная сборка (`aur/`)
- **Назначение**: Сборка из исходников GitHub
- **Зависимости**: `electron`, `nodejs`, `npm`
- **Процесс**: Компиляция через `npm install` и упаковка

#### Бинарная сборка (`deploy_aur/`)
- **Назначение**: Переупаковка готовых бинарных релизов
- **Зависимости**: `electron`, `wget`
- **Процесс**: Скачивание `.pkg.tar.zst` из GitHub Releases

### 3. CI/CD пайплайн (GitHub Actions)
#### Джоб 1: `build-bin`
- **Функция**: Подготовка и валидация PKGBUILD
- **Действия**: Патчинг с commit SHA, генерация .SRCINFO

#### Джоб 2: `create-binary-release`
- **Функция**: Создание бинарного релиза и обновление AUR
- **Действия**: 
  - Автоматическое инкрементирование `pkgrel` в `deploy_aur/PKGBUILD`
  - Сборка `.pkg.tar.zst` пакета
  - Публикация в GitHub Releases
  - Обновление AUR репозитория через SSH

#### Джоб 3: `deploy-to-aur`
- **Функция**: Публикация исходной версии в AUR
- **Действия**: Обновление основного AUR пакета

### 4. Система версионирования
- **pkgver**: Основная версия (1.1.3)
- **pkgrel**: Автоматически инкрементируется для обновлений
- **Теги**: `binary-{commit_sha}` для GitHub Releases

### 5. Launcher и конфигурация
- **Launcher**: `/usr/bin/perplexity` с поддержкой Wayland/X11
- **Конфигурация**: `~/.config/Perplexity/perplexity.conf`
- **Автоматическая детекция**: Сессии, D-Bus, переменных окружения
- **Флаги**: Настраиваемые через `ELECTRON_ARGS`

### 6. Интеграция с системой
- **Desktop Entry**: Стандартный `.desktop` файл
- **Иконки**: Интеграция в `hicolor` тему
- **Пути**: Соответствие FHS (Filesystem Hierarchy Standard)
- **Отключение автообновлений**: В файле `src/todesktop.json` для Perplexity полностью отключены автообновления (`autoUpdater: false`, `autoUpdates: все параметры false`). Пункт 'Check for updates' полностью удалён из всех меню трея (rightClick.menu и leftClick.menu). Трей и остальные пункты меню (Open, Launch at startup, Quit) сохранены. Это соответствует best practice для Electron/ToDesktop: обновления не проверяются, пользователь не видит пункт проверки в трее, но трей работает.

---

## Структура проекта

```
project-root/
├── .github/workflows/     # CI/CD автоматизация
│   └── build_and_publish.yml
├── docs/                  # Документация
│   ├── architecture.md
│   └── todos.md
├── src/                   # Исходники Electron-приложения
│   ├── main.js
│   ├── package.json
│   └── node_modules/
├── aur/                   # Исходная сборка AUR
│   ├── PKGBUILD           # Сборка из GitHub исходников
│   ├── launcher.sh        # Launcher скрипт
│   ├── perplexity.desktop
│   └── perplexity.png
├── deploy_aur/            # Бинарная сборка AUR
│   ├── PKGBUILD           # Переупаковка бинарных релизов
│   ├── launcher.sh
│   ├── perplexity.desktop
│   ├── perplexity.png
│   └── default.conf       # Конфигурация по умолчанию
├── usr/bin/perplexity     # Установленный launcher
├── usr/share/applications/perplexity.desktop
├── usr/share/pixmaps/perplexity.png
├── README.md
└── LICENSE
```

---

## Ключевые нюансы и требования

- **Нет необходимости в энтерпрайз-тестах** — проект личный, тесты минимальны.
- **Иконки** — всех размеров, интеграция через .desktop и PKGBUILD.
- **Конфиг** — хранится в `~/.config/perplexity/`, создаётся автоматически.
- **Запуск** — из терминала как `perplexity`, в системе как "Perplexity".
- **Пути установки** — соответствуют стандартам Arch Linux.
- **Обновление** — вручную или через автоматизацию.
- **Вся логика управления переменными и флагами — в launcher.sh**.
- **.desktop-файл не содержит переменных, только вызов launcher**.

---

## Используемые инструменты и технологии

### Основные технологии
- **Electron 33.3.2** (системная зависимость)
- **ToDesktop Runtime 1.5.7-beta.1** (базовая платформа)
- **Node.js/npm** (управление зависимостями)
- **Bash** (launcher с поддержкой Wayland/X11)

### Инфраструктура и автоматизация
- **GitHub Actions** (полная автоматизация CI/CD)
- **makepkg** (сборка пакетов Arch Linux)
- **SSH** (безопасная публикация в AUR)
- **GitHub Releases** (хостинг бинарных пакетов)

### Инструменты сборки
- **ArchLinux containers** (изолированная среда сборки)
- **bsdtar** (распаковка пакетов)
- **curl/wget** (загрузка релизов)
- **git** (управление версиями AUR)

---

## Безопасность и мониторинг

### Безопасность CI/CD
- **SSH ключи**: Безопасное хранение в GitHub Secrets
- **Изолированные контейнеры**: ArchLinux containers для сборки
- **Непривилегированные пользователи**: Сборка от имени `builder`
- **Временные файлы**: Использование `mktemp` для безопасности

### Мониторинг и отладка
- **GitHub Actions логи**: Полная трассировка сборки
- **Артефакты**: Сохранение `.pkg.tar.zst` файлов
- **Версионирование**: Автоматическое отслеживание изменений
- **Rollback**: Возможность отката через git history

---

## Статус выполнения (TODO)

### ✅ Выполнено
- [x] Структура исходников и автоматизация сборки
- [x] Launcher с поддержкой переменных и конфига
- [x] Чистый .desktop-файл
- [x] Интеграция иконок
- [x] Документация по сборке и публикации
- [x] Автоматизация CI/CD для AUR публикации
- [x] Поддержка Wayland и X11 в launcher
- [x] Исправление критических ошибок сборки
- [x] Двойная система сборки (исходная/бинарная)
- [x] Автоматическое версионирование через pkgrel
- [x] Интеграция с GitHub Releases
- [x] Реорганизация CI/CD джобов для логичности
- [x] Исправление циклических зависимостей в workflow

### 🔄 В планах
- [ ] Автоматизация скачивания и обновления исходников
- [ ] Расширение поддержки других дистрибутивов (Debian/Ubuntu)
- [ ] Мониторинг обновлений upstream Perplexity
- [ ] Автоматические тесты функциональности

---

## Диаграмма архитектуры

```
┌─────────────────┐    ┌──────────────────┐    ┌─────────────────┐
│   GitHub Push   │───▶│   build-bin      │───▶│ Binary Artifact │
└─────────────────┘    └──────────────────┘    └─────────────────┘
                                │                        │
                                ▼                        ▼
                       ┌──────────────────┐    ┌─────────────────┐
                       │create-binary-    │───▶│ GitHub Release  │
                       │    release       │    └─────────────────┘
                       └──────────────────┘             │
                                │                        │
                                ▼                        ▼
                       ┌──────────────────┐    ┌─────────────────┐
                       │   deploy-to-aur  │───▶│   AUR Package   │
                       └──────────────────┘    └─────────────────┘
                                │                        │
                                ▼                        ▼
┌─────────────────┐    ┌──────────────────┐    ┌─────────────────┐
│ User installs   │◀───│   pacman/yay     │◀───│  ArchLinux      │
│   perplexity    │    │                  │    │   Repository    │
└─────────────────┘    └──────────────────┘    └─────────────────┘
```

---

## Заключение

Проект **Perplexity Native для Linux** представляет собой комплексное решение для автоматизированной сборки и распространения Electron-приложения через AUR. Архитектура построена на принципах:

- **Автоматизации**: Полный CI/CD цикл от коммита до публикации
- **Надежности**: Двойная система сборки и автоматическое версионирование
- **Безопасности**: Изолированные контейнеры и безопасное хранение ключей
- **Масштабируемости**: Готовность к расширению на другие дистрибутивы

Текущая версия **1.1.3-2** демонстрирует стабильность архитектуры и готовность к дальнейшему развитию.

---

## Best practice: безопасное использование временных файлов и директорий (mktemp)

**Почему важно:**
- Ручное создание временных файлов (например, echo data > /tmp/myfile) небезопасно: возможна подмена файла, коллизии, race condition.
- mktemp гарантирует уникальность имени и выставляет безопасные права доступа.
- Это стандарт для всех современных shell-скриптов, CI/CD и автоматизаций.

**Шаблон для Bash-скриптов:**
```sh
# Временный файл
TMPFILE=$(mktemp)
echo "Данные" > "$TMPFILE"
# ... работа ...
rm "$TMPFILE"

# Временная директория
TMPDIR=$(mktemp -d)
# ... работа ...
rm -r "$TMPDIR"
```

**Рекомендация для проекта:**
- Использовать mktemp во всех новых автоматизациях, скриптах, CI/CD, где требуется временное хранение данных.
- Не использовать фиксированные имена файлов/директорий в /tmp.
- В случае доработки launcher.sh, PKGBUILD, CI/CD — применять этот шаблон.